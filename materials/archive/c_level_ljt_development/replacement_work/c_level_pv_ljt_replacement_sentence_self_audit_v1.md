# Replacement sentence self-audit v1

Status: internal self-audit of first-draft replacement sentence pairs.

Files audited:

- `c_level_pv_ljt_replacement_sentence_pairs_v1.tsv`
- `c_level_pv_ljt_replacement_candidate_pool_v1.tsv`

## Summary

The draft contains 17 replacement PV pairs and 34 sentence rows.

Mechanical checks passed:

- 17 acceptable rows
- 17 unacceptable rows
- all sentences 5-9 words
- target PV present in every sentence
- target PV never begins in word position 1 or 2

Validity check:

- 1 pair is provisionally production-level after native review: `take over`
- 9 pairs are in the 80-89 range and need minor or moderate revision
- 7 pairs are below 80 and should not be advanced without major revision or replacement

## Pairs below 80

| draft | PV | score | problem | recommended action |
|---|---:|---:|---|---|
| `draft_001` | `turn out` | 76 | The unacceptable sentence may be acceptable under an identity/evidential reading. | Replace with `set up`, `take on`, or rewrite using a stronger property complement contrast. |
| `draft_003` | `figure out` | 72 | The unacceptable object recreates broad concrete-object oddness. | Replace with `fill out` if a stable anchor item is acceptable, or redesign `figure out` with a clause complement. |
| `draft_004` | `step back` | 78 | `step back from the verdict` may be interpretable as figurative distancing. | Rewrite or replace with `put off`, `bring up`, or `take on`. |
| `draft_008` | `go over` | 70 | The unacceptable item depends too much on animate/object mismatch. | Replace with `bring up`, `put off`, or `set up`; do not keep current pair. |
| `draft_011` | `carry on` | 76 | The contrast is more about obstacle plausibility than the PV sense. | Use as a practice/anchor item only, or replace with `go through` or `set off`. |
| `draft_013` | `move on` | 68 | It is difficult to create an unacceptable topic-transition item without broad object cues. | Replace with `put off`, `bring up`, or `pass on`. |
| `draft_014` | `build up` | 70 | Still too close to the transparent increase/decrease cue problem. | Replace with `bring down`, `set off`, or `hold up`; avoid simple accumulation items. |

## Better current candidates

These pairs are strongest in the first draft:

- `draft_017` / `take over` / 90
- `draft_002` / `take up` / 88
- `draft_015` / `shut down` / 88
- `draft_016` / `turn down` / 88
- `draft_009` / `bring out` / 86
- `draft_010` / `throw out` / 86

These are usable for further review after targeted polishing:

- `draft_005` / `give in` / 84
- `draft_007` / `sort out` / 84
- `draft_012` / `open up` / 84
- `draft_006` / `play out` / 82

## Next revision pass

For v2, do not simply edit the below-80 unacceptable nouns. First decide whether
the PV sense itself can support a 90-point near-miss.

Recommended v2 substitutions:

- replace `turn out` with `set up` or `take on`
- replace `figure out` with `fill out` only if an easier anchor item is desired; otherwise use `bring up`
- replace `step back` with `put off`
- replace `go over` with `bring up`
- replace `carry on` with `go through`
- replace `move on` with `pass on`
- replace `build up` with `set off` or `hold up`

After v2 drafting, rerun the same mechanical checks and require at least 12 of
17 replacement pairs to score 85+ before building a full 48-target provisional
master.

